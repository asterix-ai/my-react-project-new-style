import { projectId } from '../contexts/FirebaseContext';

// This file can be used to export the projectId for services that need it
// and potentially other non-context-dependent Firebase utilities.
export { projectId };
